# A Homepage Ji Typography Alignment

## Scope

Refine the A (`design-previews/weijie`) homepage profile hierarchy and publication resource links. Preserve the current portrait, monochrome social icons, motto, adaptive publication images, filters, publication metadata, and all unrelated sections.

## Profile hierarchy

- Increase the desktop name from 165% to 180% of the 16 px body size.
- Use `Changyi Li (李长艺)` as a single heading with one consistent size and weight.
- Render `Ph.D. Student` as a separate 110% bold profile title, following the hierarchy used by Ji Jiaming's homepage.
- Keep the institutions as normal body text immediately below the profile title.
- Remove `Shanghai, China` entirely.
- Preserve the existing spacing before the email, social icons, and motto.
- On screens up to 700 px wide, return the name to 165% to avoid crowding while retaining the 110% bold profile title.

## Publication resource links

- Match Ji Jiaming's publication-link treatment: 100% of the body font size, normal 400 weight, and the existing blue link color.
- Remove whitespace between adjacent bracketed resources, producing forms such as `[Page][Paper][Code][Models][Data]`.
- Preserve the current resource order for every paper.
- Keep the live GitHub star badge at the end with its existing small left margin so it remains visually separate from the bracketed resources.
- Do not alter link destinations, star-count behavior, publication titles, authors, venues, or the Spotlight badge.

## Verification

- Capture desktop and 390 px mobile screenshots.
- Confirm the desktop name computes to 28.8 px and the mobile name to 26.4 px with a 16 px body font.
- Confirm the profile title computes to 17.6 px and is bold at both sizes.
- Confirm `Shanghai, China` no longer appears.
- Confirm publication resource links compute to 16 px and 400 weight.
- Confirm adjacent bracketed links have no rendered whitespace while star badges remain separated.
- Confirm no horizontal overflow and that all existing publication filters still return 4, 3, and 3 items.
